// Generated by CoffeeScript 1.4.0
(function() {
  var Colormap, Gradient;

  Gradient = require('./gradient');

  Colormap = (function() {

    function Colormap() {
      this.initGradients();
      this.initValues();
    }

    Colormap.prototype.initGradients = function() {
      this.red = new Gradient();
      this.green = new Gradient();
      return this.blue = new Gradient();
    };

    Colormap.prototype.initValues = function() {
      var b, g, r;
      r = this.red;
      g = this.green;
      b = this.blue;
      r.add(0, 0);
      r.add(0.34, 1);
      r.add(1, 1);
      g.add(0, 0);
      g.add(1, 1);
      b.add(0, 0);
      b.add(0.65, 0);
      b.add(0.98, 1);
      b.add(1, 1);
      r.build();
      g.build();
      return b.build();
    };

    Colormap.prototype.getValue = function(level) {
      var b, g, r;
      r = this.red.gradient[level];
      g = this.green.gradient[level];
      b = this.blue.gradient[level];
      return (255 << 24) | (r << 16) | (g << 8) | b;
    };

    return Colormap;

  })();

  if (typeof module !== "undefined" && module !== null) {
    module.exports = Colormap;
  }

}).call(this);
