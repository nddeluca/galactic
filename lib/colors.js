// Generated by CoffeeScript 1.4.0
(function() {
  var Colormap, colors;

  Colormap = require('./colormap');

  colors = {
    grayscale: function(stretchData, colorData) {
      var i, value;
      i = stretchData.length;
      while (i--) {
        value = stretchData[i];
        colorData[i] = (255 << 24) | (value << 16) | (value << 8) | value;
      }
      return void 0;
    },
    heat: function(stretchData, colorData) {
      var i, map, _results;
      map = new Colormap();
      i = stretchData.length;
      _results = [];
      while (i--) {
        _results.push(colorData[i] = map.getValue(stretchData[i]));
      }
      return _results;
    }
  };

  if (typeof module !== "undefined" && module !== null) {
    module.exports = colors;
  }

}).call(this);
